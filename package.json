{
  "name": "toolbox",
  "version": "0.1.2",
  "description": "Forget-Me-Knot ToolBox — your personal service dashboard",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "tauri dev",
    "tauri:build": "tauri build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^22.10.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.7.0"
  },

  "allowScripts": {
    "electron": true,
    "esbuild": true
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-autostart": "^2.5.1"
  }
}
